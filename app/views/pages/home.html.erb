<div class="container">
  Home Page
  <% if user_signed_in? %>
    <h2>Hi <%= @user.first_name %></h2>
  <% else %>
    <%= link_to 'Sign in', new_user_session_path %>
  <% end %>
  <%= link_to 'New Project', new_project_path %>
</div>
