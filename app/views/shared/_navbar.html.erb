<nav class="bg-white shadow mb-1">
  <div class="max-w-3xl mx-auto py-3 px-6 md:px-0 md:flex md:justify-between md:items-center">
    <div class="flex justify-between items-center">
      <% if user_signed_in? %>
        <div class="flex items-center">
          <%= image_tag "logo.png", class: "h-8 shadow rounded-full", alt: "user photo" %>
          <a href="#" class="text-gray-500 text-xl hover:text-gray-700 ml-4"><%= @user.full_name %></a>
        </div>
      <% else %>
        <div class="flex items-center">
          <%= image_tag "logo.png", class: "h-8 shadow rounded-full", alt: "user photo" %>
          <a href="#">BeetleByte</a>
        </div>
      <% end %>
      <%# Mobile menu button %>
      <div class="flex md:hidden">
        <button type="button" class="text-gray-500 hover:text-gray-600 focus:outline-none focus:text-gray-600" aria-label="toggle-menu">
          HMB
        </button>
      </div>
    </div>
  </div>
</nav>
